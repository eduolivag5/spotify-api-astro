---
import MediaCard from "../MediaCard.astro";

const { id, name, image } = Astro.props;
---
<MediaCard 
  href={`/artist/${id}`}
  title={name}
  subtitle="Artista"
  image={image}
  type="artist"
  transitionName={`artist-image-${id}`}
/>