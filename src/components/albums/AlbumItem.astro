---
import MediaCard from "../MediaCard.astro";

const { album } = Astro.props;
---
<MediaCard 
  href={`/album/${album.id}`}
  title={album.name}
  subtitle={album.album_type === 'album' ? 'Álbum' : 'Single'}
  image={album.images[0]?.url}
  type="album"
  transitionName={`album-image-${album.id}`}
/>